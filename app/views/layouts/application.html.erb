<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "URL Shortening Service" %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Icons and Favicon -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-b from-indigo-100 via-purple-100 to-pink-100 text-gray-900 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-white shadow-lg">
      <div class="container mx-auto px-4 py-6">
        <h1 class="text-3xl font-bold text-center text-indigo-600">
          <%= content_for?(:title) ? yield(:title) : "URL Shortening Service" %>
        </h1>
      </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 container mx-auto px-6 py-10">
      <div class="bg-white rounded-lg shadow-md p-6">
        <%= yield %>
      </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-6">
      <div class="container mx-auto text-center">
        <p class="text-sm">
          © <%= Time.now.year %> URL Shortening Service. Built with ❤️ using Ruby on Rails.
        </p>
      </div>
    </footer>
  </body>
</html>
